apiVersion: v1
kind: Service
metadata:
  name: service-a

spec:
  type: NodePort
  selector:
    service: service-a

  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
